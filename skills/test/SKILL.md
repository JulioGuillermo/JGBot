---
name: test_skill
description: A testing skill that greets the user.
metadata:
  author: JulioGuillermo
  version: "1.0"
---

## Usage

Command: **test_skill**
Arguments: `{ "name": "string" }`

## Content

This skill is used to verify the agent's ability to pass arguments to a local function.
When triggered, it must return a string following the pattern: "Hello, [name]!".
